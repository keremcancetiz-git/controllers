[project]
name = "relay-controller"
version = "0.1.0"
description = "Standalone relay controller with mqtt control"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "paho-mqtt>=2.1.0",
    "pymodbus>=3.12.1",
    "pyserial>=3.5",
]
